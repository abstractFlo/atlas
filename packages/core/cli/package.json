{
  "name": "@abstractflo/atlas-cli",
  "version": "1.1.0",
  "description": "> TODO: description",
  "author": "Florian Uhlrich <f.uhlrich@abstractmedia.de>",
  "homepage": "",
  "license": "MIT",
  "main": "dist/atlas-cli.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*",
    "stubs/*"
  ],
  "bin": {
    "atlas": "dist/atlas-cli.js"
  },
  "scripts": {
    "prebuild": "yarn clean",
    "build": "rollup -c",
    "postbuild": "yarn local:pack && yarn types && rimraf ./dist/packages",
    "prewatch": "yarn clean",
    "watch": "rollup -cw",
    "clean": "rimraf {dist,build,types,*.d.ts,*.tgz}",
    "types": "api-extractor run --local --verbose",
    "local:pack": "yarn pack --filename cli.tgz"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^13.0.2",
    "@swc/core": "^1.2.64",
    "@types/yargs": "^17.0.2",
    "eslint": "^7.30.0"
  },
  "dependencies": {
    "colorette": "^1.2.2",
    "pretty-ms": "^7.0.1",
    "rollup": "2.52.7",
    "yargs": "^17.0.1"
  }
}
